/**
 * Λιανική (retail bucket) display — matches the fuchsia "Σύστημα" cue on customer cards,
 * keeps Greek labels readable (no forced uppercase / wide tracking / mono).
 */
export const retailEndClientPillClass =
  'inline-flex max-w-full items-center truncate rounded-md border border-fuchsia-200 bg-fuchsia-50 px-2 py-0.5 text-[11px] font-semibold text-fuchsia-900 tracking-tight';

export const retailOrderBagIconClass = 'text-fuchsia-600 shrink-0';
